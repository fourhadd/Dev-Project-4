name: auth_app
description: Auth Flow + Advanced State (Cubit/flutter_bloc), clean architecture
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  flutter_screenutil: ^5.9.3
  go_router: ^14.2.0
  shared_preferences: ^2.2.3
  geolocator: ^12.0.0
  get_it: ^7.7.0
  dio: ^5.4.3+1
  geocoding: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
